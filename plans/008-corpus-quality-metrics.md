# Plan008: Freeze the shared corpus and implement quality metrics

## Status

P1; effort L; risk MED. IN PROGRESS; Plan007 is reviewed DONE at source baseline `0b032fa8b9ded947c16e9b62ddf4e897dcb3f49a`. Its source diff, exact archived adapter, actual parity evidence, source datasets and isolated evaluation environment were reconciled before dispatch. Same isolated worktree/branch. Reviewer owns plans; source commits after review, no merge/push. This completes the corpus/numerical-metric prerequisite of parent001, not an optimization or final model qualification.

## Existing evidence and resources

Pinned source downloads already exist under `.build/flash/corpora/source-recon/`, with URLs/sizes/hashes in `sources.json`. Recompute hashes before use; reproduce from those exact URLs if absent. Raw external data remains ignored, never added to git.

- GSM8K commit3101c7d5072418e28b9008a6636bde82a006892c: train7473 rows, SHA25617f347dc51477c50d4efb83959dbb7c56297aba886e5544ee2aaed3024813465; test1319 rows, SHA2563730d312f6e3440559ace48831e51066acaca737f6eabec99bccb9e4b3c39d14. MIT license bytes/hash are in sources.json.
- HumanEval commit6d43fb980f9fee3c892a914eda09951f772ad10d: data/HumanEval.jsonl.gz,164 tasks, compressed SHA256b796127e635a67f93fb35c04f4cb03cf06f38c8072ee7cee8833d7bee06979ef. MIT. Freeze official execution/scorer source identity too; do not execute generated code in this plan.
- Official HumanEval scorer modules are already downloaded from that same commit and remain unexecuted: `humaneval-evaluation.py`3438B/SHA256e926d5e9b8f1ec040096cd5952e8cf79f8dc0b7fb80ba6667ce604ffea0397bc, `humaneval-execution.py`6582B/SHA25679901c6f5b59701c465b164aed290fa53abc35abc80a5378fc10f4dac1f9a84c, and `humaneval-data.py`1529B/SHA256e1351f0ded89b072490180ce94ce33b88bac4e38b26f08eb26ab0714586585ab. URLs and identities are in `source-recon/humaneval-scorer-sources.json`. Recording these modules does not certify sandboxing or scorer execution.
- XNLI repository commit9624695001060244cf8d5d893b5236093a21a42c points to https://dl.fbaipublicfiles.com/XNLI/XNLI-1.0.zip. Actual archive17865352B, SHA2564ba1d5e1afdb7161f0f23c66dc787802ccfa8a25a3ddd3b165a35e50df346ab1. License CC-BY-NC-4.0, preserved separately. TSV dev2490 English rows/830 promptIDs; test5010/1670. Exact members/hashes in xnli-selection-recon.json. promptID identifies a premise; pairID a particular hypothesis. Each premise has three hypotheses. Use first English pair encountered per selected promptID, then matching pairID in en/fr/zh; do not count three hypotheses as three independent premises.
- System Python3.9 has no NumPy. A read-only bundled Python3.12.14 exists at `/Users/imdad/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3`. The reviewer created separate ignored `.build/flash/eval-env` from that interpreter and installed only the exact NumPy wheel below with --no-index --no-deps. Verify/reuse it via its `reviewer-setup.json`; implement reproducible fresh setup and existing-environment verification, not blind replacement. Do not alter system or `/Users/imdad/models/mlx-env`.
- Pin NumPy2.3.5 CPython3.12 macOS14 arm64 wheel:5088378B, SHA256612a95a17655e213502f60cfb9bf9408efdc9eb1d5f50535cc6eb365d11b42b5, URL https://files.pythonhosted.org/packages/5b/e1/1ee06e70eb2136797abe847d386e7c0e830b67ad1d43f364dd04fa50d338/numpy-2.3.5-cp312-cp312-macosx_14_0_arm64.whl. This is evaluation tooling only. Record interpreter/package identities; bound BLAS threads locally.

The exact wheel is already downloaded and hash-verified alongside `numpy-wheel.json` in source-recon. Only the isolated evaluation environment described above was created; existing Python environments were not changed. `split-preflight.json` records7473 unique GSM8K train questions,1503 in the declared development hash partition, and no normalized-question overlap with the first200 final test cases. XNLI has830 unique dev premises,150 in that development partition, and no overlap with the selected200 final test premises. These are independent source preflights; the implementation must still verify the frozen selected suite itself.

## Scope

Tools/flash/corpus.py,metrics.py,test_corpus.py,test_metrics.py, eval environment setup/lock and focused gate extensions; versioned schemas, suite manifest and small repository-authored fixtures under Tools/fixtures/flash. Reuse Plan007 tokenizer/capture APIs. No Swift math, model weights, neuron masks, predictor, runtime policy, serving, final qualification inference or public performance claims.

Use the reconciled helper name `Tools/flash/tokenize_corpus.py`; do not introduce a `tokenize.py` module that shadows the Python standard library. Use an explicitly versioned contract for later scored-generation prompts. Do not claim that arbitrary teacher-forced windows within a task prompt are scored-task outcomes. If freezing the exact task prompt IDs needs tokenizer-only support beyond007's bounded teacher windows, report that concrete interface gap for reconciliation; do not silently raise the256-token teacher warmup bound or substitute/pad a prompt.

## Corpus contract

1. Freeze sources, licenses, selectors, normalization and scorer contracts before candidate numerical experiments. Normalize NFKC + collapsed whitespace for duplicate/partition checks only; tokenize the exact original bytes. Use canonical structured message serialization to avoid concatenation ambiguity. Store raw and normalized hashes.
2. Training/development use public train/dev sources and independently authored prose/code/structured examples. Partition original document/premise groups by integer SHA256(normalized partition text) modulo5:0 development, others training. All translations/hypotheses of a premise share the English-premise key. Exclude every selected final task/text/premise from both. Reject duplicate/leaking documents; shared vocabulary is allowed.
3. Initial teacher-forced development cohort: at least512 positions across all five categories(prose,code,math,multilingual,structured). Freeze enough short whole documents; use predeclared scoring windows and reject insufficient token length rather than padding. Suggested deterministic quotas: four32-position docs in each category, with complete multilingual families if that increases the total. Training selection is frozen too, but its model capture can wait for the oracle decision.
4. Final teacher-forced cohort: at least4096 predicted positions,100 documents,20 documents/512 positions per category. Select first eligible documents in frozen source order with declared warmup/score bounds. Add genuinely distinct authored prose/code documents where source material is insufficient; no bulk near-duplicate filler. Preserve exact selection/rejection reasons. Entire final input manifests may be prepared/tokenized, but final inference stays locked.
5. Final scored cohorts fixed exactly as parent001: first200 GSM8K test rows; all164 HumanEval tasks ordered numerically; first200 unique XNLI test promptIDs in English file order, first pair per premise and its en/fr/zh counterparts(600 tasks);200 deterministic authored JSON extraction/tool-argument tasks with seed1. Store actual expected answers/tests and prompt/scorer contracts in ignored prepared data with manifest hashes/case selectors, not unlicensed copied source in git. One greedy answer per case, reasoning off, frozen output/time limits. Scored-task execution is a later step; mark it unavailable until its real scorer/sandbox exists, never stub PASS.
6. Use Plan007 to freeze exact text/chat IDs and <=64-position shards with <=256 warmup. Pass its explicit `--binary .build/flash/runs/diagnostic-reference-corpus-v2/bin/slotstream` so subsequent candidate builds cannot invalidate frozen inputs. Bind tokenizer adapter and immutable reference anchor separately. Record whole corpus/shard ordering, splits, cases, categories, selected ranges, all source/tokenizer/build/scorer hashes and denominators. `corpus.py freeze --output Tools/fixtures/flash/suite.json` refuses overwrite. `validate` rechecks sources, selectors, text and token hashes, coverage and disjointness; no auto-repair of a frozen suite. Data changes require a new version, not rewriting successful evidence.

## Metric contract

Implement streamed full-vocabulary comparison of aligned FP32 logits. Validate the frozen corpus and both capture identities/coverage before metrics. Each row is joined by split/document/position/input/next-token IDs and vocabulary; no directory-wide best-effort aggregation. Chunk<=16 rows, bounded scratch, reject missing/duplicate/extra/nonfinite rows and artifact drift.

Compute stable log-softmax in FP32 (subtract row maximum before logsumexp), then KL(p_reference||p_candidate) and NLL sums in FP64. Mean values are token-weighted. PPL ratio is exp(mean candidate NLL minus mean reference NLL), not an average of document perplexities. Compute nearest-rank p99, top1 agreement and category results. Fix and test a small roundoff band for negative KL; report tolerated raw values, reject materially negative values, never arbitrarily clamp failures. Record NumPy/interpreter/backend identity. Document-cluster bootstrap resamples whole documents with seed17 and10000 resamples; report counts/limitations and never claim broad non-inferiority from this fixed suite.

The metric module reports evidence and threshold outcomes, not final qualification. Parent thresholds stay unchanged: approximate meanKL<=1e-3,p99<=1e-2,top1>=99%,PPLratio<=1.01; numerical full-neuron mean<=1e-4,p99<=1e-3,top1>=99.9%; both pooled/per-category. Missing required scored-task or final-cohort evidence cannot pass overall qualification.

## Tests and verification

Corpus: source hash/version/license mismatch, duplicate case/text, translated-family leakage, forged split/partition, reordered selectors, empty/incomplete categories, insufficient token length, offset mismatch, changed tokenizer/shards, overwrite refusal, final capture denial. Metric: independently computed two-/three-class KL and direction, identical distributions, shift invariance, known NLL/PPL ratio, weighted aggregation, nearest-rank ties, document-cluster bootstrap determinism, malformed/nonfinite/missing rows and wrong vocabulary/targets. Tests must fail on altered real calculations, not compare two invocations of the same helper.

Run setup in the isolated environment, all Flash tests with that interpreter, corpus freeze/validate, metric self-tests, and actual full-vocabulary comparison of the accepted observer-off/on data to prove exact zero KL/100%top1 without another model load. Then capture the frozen development corpus using the accepted Plan007 adapter in serial14GB/17GB/2-second/terminal-policy runs and compare reference outputs to themselves as an integration control only. Keep observer capture data separate from performance timing. Do not train or mask until this prerequisite is reviewed.

Done means a tested frozen corpus and actual aligned metric pipeline with explicit still-unavailable scored/final gates. Preserve the accepted reference and source data. Stop on source drift, leakage, unsupported tokenization, missing evidence, resource failure or numerical mismatch. Return STATUS/STEPS/STOPPED BECAUSE/FILES CHANGED/NOTES with commands, hashes, counts and limitations. Commit only after reviewer approval.
