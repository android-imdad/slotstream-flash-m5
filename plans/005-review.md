# Plan005 review — implementation approved; reference pending Plan006

> Historical review/checkpoint at the stated source and evidence versions. Later completion, rejection and remaining work are reconciled in [the current index](README.md) and [JANG Flash findings](../docs/JANG-FINDINGS.md). Preserve the original evidence; do not treat historical active-work wording as a new task.

Preserved source commit: `2a25359c701a78c75c3c69cbdb36d0a43d5af4b4`.

Two formal review rounds resolved bounded manifest/copy/metadata accounting, complete state identity hashing, finite tensor checks, exact policy and environment joins, actual resident/miss callback coverage, checked reservation behavior and consistent input validation. Independent verification passed the fresh strict Flash gate with 99 Python tests, all 50 native groups / 27519 assertions, and the complete repository static gates. Native CLI boundary refusals occur before model allocation.

The reviewer independently validated the prior five-process review1 cohort, including direct logit byte equality, all defined state/routing fields, and argmax reconstructed from the full vocabulary. All134 reported resident splits had two nonempty branches. A final-source cohort then passed observer-off and produced complete native observer-on output, but the older Plan002 launcher falsely failed on zero current footprint after normal process exit. This cohort remains invalid as a complete externally monitored reference. Do not combine successful arms across cohorts.

Root's tiny-child probe proved that Darwin retains a positive terminal lifetime peak and exit timestamp until reaping even when current physical footprint is zero. Plan006 repairs this distinct launcher prerequisite and strengthens final-peak evidence. Plan005 implementation is preserved, but reference acceptance and its DONE status await a complete fresh cohort after006.

Evidence: `.build/flash/runs/reviewer-flash-gate-final/`, `reviewer-flash-full-final/`, `reviewer-flash-candidate/`, `capture-parity-review1/`, `reviewer-capture-final/`, and `sampler-exit-recon/`.

The adapter is explicitly diagnostic-only: two arbitrary-ID documents/three observed positions, maximum64 positions and256-token warmup. It is not the full natural-language corpus, training data, sparse oracle, predictor, or end-to-end performance qualification. No model/default/math change, merge or push.

## Final acceptance after Plan006

Fresh independent cohort `.build/flash/runs/reviewer-capture-terminal/` passed all five processes with exactly one valid terminal sample each. Root revalidated every native report and launcher receipt, directly compared full logit bytes and state/routes across observer off/on/resident-split arms, and independently reconstructed argmax from full-vocabulary files. Ordinary archived/current CLI parity passed. The accepted immutable archive is `.build/flash/runs/diagnostic-reference/`. Plan005 is DONE at its bounded diagnostic scope; the full natural corpus and quality protocol are still pending in parent001.
